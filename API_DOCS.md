1 Prerequisites (Python, Git)
- Python 3.x installed
- Git installed
- A terminal (Linux/macOS Terminal, or Windows WSL/Git Bash)

2 Setup steps (clone, venv, install deps)

 Clone the repo
```bash
git clone https://github.com/OluwatosinSoyemi/Git-Ops-Style
cd <REPO_FOLDER>

3 Run lint/tests commands

- flake8 app.py test_app.py
- pytest test_app.py -v
- pip install -r requirements.txt

4 Branch workflow (feature/TRELLO-###…)

5 PR creation steps

6 Troubleshooting (common issues)
